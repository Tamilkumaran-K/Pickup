{
  "name": "@dropflow/desktop",
  "version": "1.0.0",
  "type": "module",
  "description": "DropFlow Native Desktop Client for Windows & macOS with Zero-Click Auto-Save",
  "main": "./dist/main.js",
  "scripts": {
    "prebuild": "npm --prefix ../shared run build",
    "build": "tsc",
    "start": "tsc && electron dist/main.js"
  },
  "dependencies": {
    "@dropflow/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "electron": "^30.0.9",
    "typescript": "^5.4.5"
  }
}
